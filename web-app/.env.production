# Production API base URL used when building on Vercel.
# This points at the Render-hosted Django API (see backend/render.yaml).
# Override in Vercel → Project → Settings → Environment Variables (key VITE_API_URL)
# if your Render service is named differently.
VITE_API_URL=https://vikoba-geb8.onrender.com/api/v1
VITE_APP_NAME=Vikoba Kidigitali
